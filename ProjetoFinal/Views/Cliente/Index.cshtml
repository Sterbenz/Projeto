<h3 class="page-title">
    Lista de clientes <small>Visualisar clientes</small>
</h3>
<ul class="nav nav-tabs">
    <li style="bottom: 2px">
        <form class="form-horizontal form-row-seperated" action="@Url.Action("Form","Cliente")" method="post">
            <button class="btn default green" type="submit">Registrar cliente</button>
        </form>
    </li>
</ul>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th>CPF</th>
            <th>Data de nascimento</th>
            <th>Email</th>
            <th>Telefone</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var cliente in ViewBag.Clientes)
        {
        <tr id="@cliente.Id">
            <th>@cliente.Id</th>
            <th>@cliente.Nome</th>
            <th class="cpf">@cliente.Cpf</th>
            <th class="data">@cliente.DataDeNascimento</th>
            <th>@cliente.Email</th>
            <th class="telefone">@cliente.Telefone</th>
            <th>
                <button class="btn default red-flamingo" onclick="Deletar(@cliente.Id, 'Cliente')" type="submit">Deletar</button>
            </th>
            <th>
                <form action="@Url.Action("Editar", "Cliente", new { cliente.Id })">
                    <button class="btn default green-jungle" type="submit">Editar</button>
                </form>
            </th>
        </tr>
        }

    </tbody>

</table>